/**
 * Defining the type and interactions of the simulated cluster of particles (system). Users
 * can provide input files containing some abstract particle names and properties that say how the
 * particles interact with each other. These will be read and interpreted by the code.
 * @param specs: Particle species. Contains the species of particles in the cluster (system) being simulated.
 * @param props: Properties of the individual particles. Includes interaction strength with all othe particles.
 * @
**/

template <typename Iterator_speciies, typename Iterator_props>

